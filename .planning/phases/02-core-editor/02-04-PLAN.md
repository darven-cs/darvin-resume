---
phase: "02"
plan: "04"
type: "execute"
wave: "4"
depends_on: ["02-01"]
files_modified:
  - frontend/src/components/MonacoEditor.vue
  - frontend/src/components/ContextMenu.vue
  - frontend/src/components/LineGutter.vue
autonomous: true
requirements:
  - EDIT-08
  - EDIT-09
  - EDIT-10

must_haves:
  truths:
    - "Markdown 标题行（# ## ###）和列表行（- * +）行首显示可点击的图标"
    - "点击图标可以折叠/展开该内容块"
    - "用户可以通过拖拽行首图标调整内容块顺序"
    - "点击行首图标弹出上下文菜单，包含上移/下移/删除操作"
  artifacts:
    - path: "frontend/src/components/ContextMenu.vue"
      provides: "上下文菜单组件（上移/下移/删除等操作）"
      min_lines: 60
    - path: "frontend/src/components/MonacoEditor.vue"
      provides: "MonacoEditor 扩展行级交互功能"
      contains: "deltaDecorations|onMouseDown"
  key_links:
    - from: "MonacoEditor.vue"
      to: "ContextMenu.vue"
      via: "行首图标点击触发"
      pattern: "showContextMenu"
    - from: "MonacoEditor.vue"
      to: "editor model"
      via: "getLineContent|getLineCount"
      pattern: "getLineContent"
---

<objective>
在 Monaco Editor 中实现行级交互功能：折叠/展开图标、拖拽排序、快捷菜单。这是 Phase 2 的最后一项，为用户提供结构化的 Markdown 内容编辑能力。

**Purpose:** 用户可以通过可视化的行首图标快速折叠内容、调整段落顺序、触发快捷操作，提升编辑效率。
**Output:** 扩展 MonacoEditor.vue + 新增 ContextMenu.vue。
**Depends on:** Plan 02-01 (Monaco Editor 实例必须可用)
</objective>

<context>
@/home/darven/桌面/dev_app/darvin-resume/.planning/phases/02-core-editor/02-01-PLAN.md
@/home/darven/桌面/dev_app/darvin-resume/.planning/phases/02-core-editor/02-CONTEXT.md
@/home/darven/桌面/dev_app/darvin-resume/.planning/phase-2/RESEARCH.md
</context>

<interfaces>
从 Plan 02-01 继承：

From frontend/src/components/MonacoEditor.vue (after plan 02-01):
```typescript
// 暴露的接口
defineExpose({
  getEditor: () => editor,
  getMonaco: () => monacoInstance,
})

// 已有的 emit
const emit = defineEmits<{
  'update:modelValue': [value: string]
  'change': [value: string]
}>()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ContextMenu.vue 上下文菜单组件</name>
  <files>frontend/src/components/ContextMenu.vue</files>
  <action>
创建通用的上下文菜单组件，用于行首快捷操作。菜单通过 Portal 渲染到 body 避免层级问题。

**文件：frontend/src/components/ContextMenu.vue**

```vue
<template>
  <Teleport to="body">
    <div
      v-if="visible"
      class="context-menu"
      :style="menuStyle"
      @click.stop
    >
      <button
        v-for="item in items"
        :key="item.id"
        class="menu-item"
        :class="{ disabled: item.disabled, danger: item.danger }"
        :disabled="item.disabled"
        @click="handleClick(item)"
      >
        <span class="menu-icon">{{ item.icon }}</span>
        <span class="menu-label">{{ item.label }}</span>
        <span v-if="item.shortcut" class="menu-shortcut">{{ item.shortcut }}</span>
      </button>
    </div>
    <!-- 点击外部关闭 -->
    <div
      v-if="visible"
      class="context-menu-overlay"
      @click="close"
      @contextmenu.prevent="close"
    />
  </Teleport>
</template>

<script setup lang="ts">
import { ref, computed, onMounted, onUnmounted, nextTick } from 'vue'

export interface MenuItem {
  id: string
  label: string
  icon?: string
  shortcut?: string
  disabled?: boolean
  danger?: boolean
  action: () => void
}

const props = defineProps<{
  items: MenuItem[]
  x: number
  y: number
  visible: boolean
}>()

const emit = defineEmits<{
  close: []
}>()

const menuStyle = computed(() => {
  // 确保菜单位于视口内
  const menuWidth = 180
  const menuHeight = props.items.length * 32 + 8

  let left = props.x
  let top = props.y

  // 右侧超出
  if (left + menuWidth > window.innerWidth) {
    left = window.innerWidth - menuWidth - 8
  }
  // 底部超出
  if (top + menuHeight > window.innerHeight) {
    top = window.innerHeight - menuHeight - 8
  }

  return {
    left: `${Math.max(4, left)}px`,
    top: `${Math.max(4, top)}px`,
  }
})

function handleClick(item: MenuItem) {
  if (item.disabled) return
  item.action()
  emit('close')
}

function close() {
  emit('close')
}

// ESC 键关闭
function handleKeydown(e: KeyboardEvent) {
  if (e.key === 'Escape') {
    emit('close')
  }
}

onMounted(() => {
  document.addEventListener('keydown', handleKeydown)
})

onUnmounted(() => {
  document.removeEventListener('keydown', handleKeydown)
})
</script>

<style scoped>
.context-menu {
  position: fixed;
  z-index: 10000;
  min-width: 160px;
  background: #252526;
  border: 1px solid #3c3c3c;
  border-radius: 6px;
  padding: 4px 0;
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.4);
}

.menu-item {
  display: flex;
  align-items: center;
  width: 100%;
  padding: 6px 12px;
  background: transparent;
  border: none;
  color: #cccccc;
  font-size: 13px;
  text-align: left;
  cursor: pointer;
  gap: 8px;
  transition: background-color 0.1s;
}

.menu-item:hover:not(.disabled) {
  background: #094771;
  color: #ffffff;
}

.menu-item.danger:hover:not(.disabled) {
  background: #5a1d1d;
  color: #ff6b6b;
}

.menu-item.disabled {
  color: #5a5a5a;
  cursor: not-allowed;
}

.menu-icon {
  width: 16px;
  font-size: 12px;
}

.menu-label {
  flex: 1;
}

.menu-shortcut {
  font-size: 11px;
  color: #8b949e;
}

.context-menu-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 9999;
}
</style>
```

**per D-15 (Context menu on gutter icon click)**
</action>
<verify>
  <automated>cd /home/darven/桌面/dev_app/darvin-resume/frontend && npm run build 2>&1 | tail -10</automated>
</verify>
<done>ContextMenu.vue 组件可正常 import，TypeScript 编译无错误</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 MonacoEditor.vue — 折叠/展开 + 拖拽排序 + 快捷菜单</name>
  <files>frontend/src/components/MonacoEditor.vue</files>
  <action>
在 Plan 02-01 创建的 MonacoEditor.vue 基础上，扩展行级交互功能。需要在 onMounted 中添加以下逻辑：

### 扩展点 1: 行首图标装饰（Gutter Icons）

在编辑器初始化后添加：

```typescript
// === 行级交互功能 per EDIT-08, EDIT-09, EDIT-10 ===
let currentDecorations: string[] = []
let foldedRanges: Map<number, { endLine: number; originalContent: string }> = new Map()

// 更新行首装饰
function updateLineDecorations() {
  const model = editor.getModel()
  if (!model) return

  const monaco = monacoInstance
  const decorations: any[] = []

  for (let i = 1; i <= model.getLineCount(); i++) {
    const lineContent = model.getLineContent(i)
    const trimmed = lineContent.trim()

    // 检测可折叠的块 per D-13
    if (isFoldable(trimmed)) {
      const isFolded = foldedRanges.has(i)
      decorations.push({
        range: new monaco.Range(i, 1, i, 1),
        options: {
          isWholeLine: true,
          // 行首图标 per D-13
          glyphMarginClassName: isFolded ? 'fold-gutter fold-gutter-collapsed' : 'fold-gutter fold-gutter-expanded',
          glyphMarginHoverMessage: {
            value: isFolded ? '**展开内容**\n点击展开 | 右键菜单' : '**折叠内容**\n点击折叠 | 右键菜单'
          },
          // 折叠范围标记
          linesDecorationsClassName: 'fold-decoration',
        },
      })
    }
  }

  currentDecorations = editor.deltaDecorations(currentDecorations, decorations)
}

function isFoldable(line: string): boolean {
  // 标题行
  if (/^#{1,6}\s/.test(line)) return true
  // 无序列表
  if (/^[-*+]\s/.test(line)) return true
  // 有序列表
  if (/^\d+\.\s/.test(line)) return true
  return false
}

// 内容变化时更新装饰
editor.onDidChangeModelContent(() => {
  updateLineDecorations()
})

// 初始更新
updateLineDecorations()
```

### 扩展点 2: 鼠标点击事件（折叠/展开 + 快捷菜单）

```typescript
// === 点击处理 per D-13, D-15 ===
let contextMenuItems = ref<MenuItem[]>([])
let contextMenuVisible = ref(false)
let contextMenuPosition = ref({ x: 0, y: 0 })

editor.onMouseDown((e: any) => {
  const target = e.target

  // 点击行首图标区域
  if (target.type === monacoInstance.MouseTargetType.GUTTER_GLYPH_MARGIN) {
    const lineNumber = target.position?.lineNumber
    if (!lineNumber) return

    const lineContent = editor.getModel()?.getLineContent(lineNumber) || ''
    const trimmed = lineContent.trim()

    if (!isFoldable(trimmed)) return

    // 左键点击：折叠/展开
    if (e.event?.leftButton) {
      if (foldedRanges.has(lineNumber)) {
        // 展开
        unfoldBlock(lineNumber)
      } else {
        // 折叠
        foldBlock(lineNumber)
      }
    }
  }
})

function foldBlock(startLine: number) {
  const model = editor.getModel()
  if (!model) return

  // 找到折叠范围（下一个同级标题或同级列表为止）
  const startContent = model.getLineContent(startLine)
  const startIndent = getIndentLevel(startContent)
  let endLine = startLine + 1

  while (endLine <= model.getLineCount()) {
    const lineContent = model.getLineContent(endLine)
    const trimmed = lineContent.trim()
    if (!trimmed) { endLine++; continue }

    const indent = getIndentLevel(lineContent)
    const isHeader = /^#{1,6}\s/.test(trimmed)
    const isList = /^[-*+]\s/.test(trimmed) || /^\d+\.\s/.test(trimmed)

    if (indent <= startIndent && (isHeader || isList)) break
    if (isHeader && /^#/.test(trimmed)) break
    endLine++
  }

  endLine--

  if (endLine > startLine) {
    const originalContent = model.getLinesContent().slice(startLine - 1, endLine).join('\n')
    foldedRanges.set(startLine, { endLine, originalContent })

    // 替换为折叠标记行
    editor.executeEdits('fold', [{
      range: new monacoInstance.Range(startLine, 1, endLine, model.getLineMaxColumn(endLine)),
      text: `${startContent} <... ${endLine - startLine} lines hidden ...>`
    }])

    updateLineDecorations()
  }
}

function unfoldBlock(startLine: number) {
  const range = foldedRanges.get(startLine)
  if (!range) return

  const model = editor.getModel()
  if (!model) return

  // 恢复原始内容
  editor.executeEdits('unfold', [{
    range: new monacoInstance.Range(startLine, 1, startLine, model.getLineMaxColumn(startLine)),
    text: range.originalContent.split('\n')[0] // 只恢复起始行，内容恢复由完整更新处理
  }])

  // 恢复完整内容
  if (model) {
    editor.executeEdits('unfold-restore', [{
      range: new monacoInstance.Range(startLine, 1, startLine + (range.originalContent.split('\n').length - 1), 1),
      text: range.originalContent
    }])
  }

  foldedRanges.delete(startLine)
  updateLineDecorations()
}

function getIndentLevel(line: string): number {
  return line.search(/\S/)
}
```

### 扩展点 3: 快捷菜单（上移/下移/删除）

在鼠标点击处理中添加右键菜单：

```typescript
// 右键点击：显示快捷菜单 per D-15
if (e.event?.rightButton) {
  const position = editor.getPosition()
  if (!position) return

  const coords = editor.getScrolledVisiblePosition(position)
  if (!coords) return

  const editorDom = editor.getDomNode()
  if (!editorDom) return

  const rect = editorDom.getBoundingClientRect()

  contextMenuItems.value = [
    {
      id: 'move-up',
      label: '上移',
      icon: '↑',
      shortcut: 'Alt+↑',
      action: () => moveLine(position.lineNumber, -1)
    },
    {
      id: 'move-down',
      label: '下移',
      icon: '↓',
      shortcut: 'Alt+↓',
      action: () => moveLine(position.lineNumber, 1)
    },
    {
      id: 'ai-rewrite',
      label: 'AI 重写',
      icon: '✦',
      shortcut: '',
      disabled: true,  // Phase 3 per deferred ideas
      action: () => {} // Placeholder
    },
    {
      id: 'delete',
      label: '删除',
      icon: '✕',
      shortcut: 'Ctrl+Shift+Delete',
      danger: true,
      action: () => deleteLine(position.lineNumber)
    },
  ]

  contextMenuPosition.value = {
    x: rect.left + coords.left + 40,
    y: rect.top + coords.top + 20,
  }
  contextMenuVisible.value = true
}

function moveLine(lineNumber: number, direction: -1 | 1) {
  const model = editor.getModel()
  if (!model) return

  const targetLine = lineNumber + direction
  if (targetLine < 1 || targetLine > model.getLineCount()) return

  const currentLine = model.getLineContent(lineNumber)
  const targetLineContent = model.getLineContent(targetLine)

  editor.executeEdits('moveLine', [
    {
      range: new monacoInstance.Range(lineNumber, 1, lineNumber, model.getLineMaxColumn(lineNumber)),
      text: targetLineContent,
    },
    {
      range: new monacoInstance.Range(targetLine, 1, targetLine, model.getLineMaxColumn(targetLine)),
      text: currentLine,
    },
  ])

  // 移动后光标位置跟随
  editor.setPosition({ lineNumber: targetLine, column: 1 })
}

function deleteLine(lineNumber: number) {
  const model = editor.getModel()
  if (!model) return

  editor.executeEdits('deleteLine', [{
    range: new monacoInstance.Range(lineNumber, 1, lineNumber, model.getLineMaxColumn(lineNumber)),
    text: '',
  }])
}

// 快捷菜单组件模板（在 MonacoEditor.vue 中添加）
// 注意：ContextMenu 在 MonacoEditor 组件内部使用 v-if 渲染
```

### 扩展点 4: 拖拽排序 per D-14

使用 Monaco Editor 的 HTML5 drag 事件（通过 gutter margin 触发）：

```typescript
// 拖拽排序 per D-14
let dragSourceLine: number | null = null

// 在编辑器 DOM 上监听 drag 事件
editor.getDomNode()?.addEventListener('dragover', (e: DragEvent) => {
  if (dragSourceLine === null) return
  e.preventDefault()
  e.dataTransfer!.dropEffect = 'move'
})

editor.getDomNode()?.addEventListener('drop', (e: DragEvent) => {
  if (dragSourceLine === null) return
  e.preventDefault()

  const position = editor.getScrolledVisiblePosition(editor.getPosition()!)
  if (!position) return

  const targetLine = Math.round(position.top / 22) // 估算行号（22px per line-height 1.6）
  if (targetLine !== dragSourceLine && targetLine > 0) {
    moveLineRange(dragSourceLine, targetLine)
  }

  dragSourceLine = null
})

function moveLineRange(sourceLine: number, targetLine: number) {
  const model = editor.getModel()
  if (!model) return

  if (sourceLine === targetLine) return

  const lines: string[] = []
  let startLine = sourceLine
  let endLine = sourceLine

  // 找到要移动的连续块
  const sourceIndent = getIndentLevel(model.getLineContent(sourceLine))

  // 向上扩展
  while (startLine > 1) {
    const prevContent = model.getLineContent(startLine - 1)
    const prevIndent = getIndentLevel(prevContent)
    if (prevIndent < sourceIndent && /^[-*+]/.test(prevContent.trim())) break
    if (/^#{1,6}\s/.test(prevContent.trim()) && prevIndent <= sourceIndent) break
    startLine--
  }

  // 向下扩展
  while (endLine < model.getLineCount()) {
    const nextContent = model.getLineContent(endLine + 1)
    const nextIndent = getIndentLevel(nextContent)
    if (nextIndent < sourceIndent && /^[-*+]/.test(nextContent.trim())) break
    if (/^#{1,6}\s/.test(nextContent.trim()) && nextIndent <= sourceIndent) break
    endLine++
  }

  const blockLines = model.getLinesContent().slice(startLine - 1, endLine).join('\n')

  editor.executeEdits('moveBlock', [
    {
      range: new monacoInstance.Range(startLine, 1, endLine, model.getLineMaxColumn(endLine)),
      text: '',
    },
  ])

  // 重新计算插入位置（可能在删除后发生变化）
  const insertLine = targetLine > endLine ? targetLine - (endLine - startLine) : targetLine

  editor.executeEdits('insertBlock', [{
    range: new monacoInstance.Range(insertLine, 1, insertLine, 1),
    text: blockLines + '\n',
  }])
}
```

**per D-13 (fold/expand icons), D-14 (HTML5 drag API), D-15 (context menu), D-16 (Monaco 内置虚拟化覆盖 10k 行，Phase 7 将进一步优化)**

> **D-16 说明**: Monaco Editor 内置虚拟化渲染，仅渲染可见区域的 DOM 节点，可自然覆盖 10,000 行文档场景。Phase 7 将根据实际性能测试数据进行额外优化（如懒加载、长文档分块渲染）。
</action>
<verify>
  <automated>cd /home/darven/桌面/dev_app/darvin-resume/frontend && npm run build 2>&1 | tail -10</automated>
</verify>
<done>MonacoEditor.vue 扩展行级交互功能，TypeScript 编译无错误</done>
</task>

</tasks>

<verification>
- 可折叠的 Markdown 块（标题、列表）行首显示图标
- 点击图标可以折叠/展开内容块
- 折叠后显示折叠标记
- 右键点击行首图标弹出上下文菜单
- 上下文菜单支持上移/下移/删除操作
- 上移/下移操作后内容正确更新
- 删除操作移除整行内容
</verification>

<success_criteria>
- 标题行（# ## ###）和列表行（- * +）显示行首图标
- 点击折叠图标隐藏后续内容（同级块范围内）
- 点击展开图标恢复折叠内容
- 右键菜单显示：上移、下移、AI 重写（禁用占位）、删除
- 上移/下移移动整块内容（包含子项）
- 删除操作即时生效
- 上下文菜单在点击外部或按 ESC 时关闭
- 编辑器内容变化时装饰自动更新
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-editor/02-04-SUMMARY.md`
</output>
