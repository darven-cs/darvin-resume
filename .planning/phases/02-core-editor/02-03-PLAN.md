---
phase: "02"
plan: "03"
type: "execute"
wave: "3"
depends_on: ["02-01", "02-02"]
files_modified:
  - frontend/src/views/EditorView.vue
  - frontend/src/composables/useDebounce.ts
  - frontend/src/utils/markdown.ts
autonomous: true
requirements:
  - EDIT-03
  - EDIT-05
  - EDIT-06

must_haves:
  truths:
    - "用户输入 Markdown 内容后，预览区在 200ms 内更新渲染结果"
    - "编辑器与预览区滚动位置大致同步"
    - "预览区显示 A4 分页边界线"
    - "响应式布局：窗口宽度 <1200px 时自动切换为单栏模式"
    - "用户可以在编辑视图和预览视图间手动切换"
  artifacts:
    - path: "frontend/src/views/EditorView.vue"
      provides: "编辑器主视图（整合 Monaco + SplitPane + A4Page）"
      min_lines: 80
    - path: "frontend/src/composables/useDebounce.ts"
      provides: "防抖工具函数"
      contains: "debounce"
  key_links:
    - from: "EditorView.vue"
      to: "MonacoEditor.vue"
      via: "v-model 绑定"
      pattern: "v-model.*content"
    - from: "EditorView.vue"
      to: "SplitPane.vue"
      via: "slot 填充"
      pattern: "<SplitPane>.*<template #left>"
    - from: "EditorView.vue"
      to: "A4Page.vue"
      via: ":content 属性绑定"
      pattern: ":content.*debouncedContent"
    - from: "EditorView.vue"
      to: "wails bindings"
      via: "GetResume(id)"
      pattern: "GetResume"
---

<objective>
将 MonacoEditor.vue、SplitPane.vue、A4Page.vue 整合到 EditorView.vue 中，实现实时预览同步（150ms 防抖，延迟<200ms）、编辑器与预览区滚动同步、以及响应式单栏切换（窗口宽度<1200px）。

**Purpose:** 用户在编辑 Markdown 时，预览区实时显示渲染结果，延迟不超过 200ms。
**Output:** 完全可用的 EditorView.vue + 防抖 composable。
**Depends on:** Plan 02-01 (MonacoEditor.vue) + Plan 02-02 (SplitPane.vue + A4Page.vue)
</objective>

<context>
@/home/darven/桌面/dev_app/darvin-resume/.planning/phases/02-core-editor/02-01-PLAN.md
@/home/darven/桌面/dev_app/darvin-resume/.planning/phases/02-core-editor/02-02-PLAN.md
@/home/darven/桌面/dev_app/darvin-resume/.planning/phase-2/RESEARCH.md
@/home/darven/桌面/dev_app/darvin-resume/frontend/src/views/EditorView.vue
@/home/darven/桌面/dev_app/darvin-resume/app.go
</context>

<interfaces>
从 Phase 1 继承的 Bridge 接口：

From wailsjs/go/main/App (auto-generated bindings):
```typescript
declare function GetResume(id: string): Promise<model.Resume>
declare function UpdateResume(id: string, jsonData: string): Promise<void>
declare function CreateResume(title: string): Promise<model.Resume>
```

From frontend/src/types/resume.ts:
```typescript
export interface Resume {
  id: string
  title: string
  markdownContent: string
  // ... 其他字段
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建防抖 Composable</name>
  <files>frontend/src/composables/useDebounce.ts</files>
  <action>
创建防抖工具函数，支持 150ms 防抖（满足<200ms 要求），用于编辑器内容变化的防抖渲染。

**文件：frontend/src/composables/useDebounce.ts**

```typescript
import { ref } from 'vue'

/**
 * 防抖 Hook
 * @param fn 要防抖的函数
 * @param delay 延迟时间（毫秒），默认 150ms per D-10
 */
export function useDebounce<T extends (...args: any[]) => any>(
  fn: T,
  delay: number = 150
) {
  const timer = ref<ReturnType<typeof setTimeout> | null>(null)
  const pending = ref(false)

  const debouncedFn = (...args: Parameters<T>) => {
    if (timer.value) {
      clearTimeout(timer.value)
    }
    pending.value = true
    timer.value = setTimeout(() => {
      fn(...args)
      pending.value = false
    }, delay)
  }

  const cancel = () => {
    if (timer.value) {
      clearTimeout(timer.value)
      timer.value = null
      pending.value = false
    }
  }

  const flush = (...args: Parameters<T>) => {
    cancel()
    fn(...args)
  }

  return { debouncedFn, cancel, flush, pending }
}

/**
 * 独立的防抖工具函数（用于 Ref 值监听）
 */
export function createDebounce<T>(fn: (val: T) => void, delay: number = 150) {
  let timer: ReturnType<typeof setTimeout> | null = null

  return (value: T) => {
    if (timer) clearTimeout(timer)
    timer = setTimeout(() => {
      fn(value)
      timer = null
    }, delay)
  }
}
```

**per D-10 (150ms debounce)**
</action>
<verify>
  <automated>cd /home/darven/桌面/dev_app/darvin-resume/frontend && npm run build 2>&1 | tail -10</automated>
</verify>
<done>useDebounce.ts 可正常 import，TypeScript 编译无错误</done>
</task>

<task type="auto">
  <name>Task 2: 整合 EditorView.vue — 完整编辑器主视图</name>
  <files>frontend/src/views/EditorView.vue</files>
  <action>
将 EditorView.vue 从占位符替换为完整的编辑器视图，整合所有组件并实现实时预览同步和响应式布局。

**EditorView.vue 实现：**

```vue
<template>
  <div class="editor-view">
    <!-- 双栏模式 (窗口宽度 >= 1200px) per D-09 -->
    <template v-if="!isSinglePane">
      <SplitPane :default-ratio="50" :min-width="300">
        <template #left>
          <div class="editor-pane">
            <div class="pane-header">
              <span class="pane-title">编辑</span>
            </div>
            <div class="editor-wrapper">
              <MonacoEditor
                ref="monacoRef"
                v-model="content"
                @change="handleContentChange"
              />
            </div>
          </div>
        </template>

        <template #right>
          <div class="preview-pane">
            <div class="pane-header">
              <span class="pane-title">预览</span>
            </div>
            <A4Page :content="debouncedContent" />
          </div>
        </template>
      </SplitPane>
    </template>

    <!-- 单栏模式 (窗口宽度 < 1200px) per D-09 -->
    <template v-else>
      <div class="single-pane-mode">
        <div class="view-tabs">
          <button
            :class="{ active: activeView === 'editor' }"
            @click="activeView = 'editor'"
          >
            编辑
          </button>
          <button
            :class="{ active: activeView === 'preview' }"
            @click="activeView = 'preview'"
          >
            预览
          </button>
        </div>

        <div class="single-pane-content">
          <div v-if="activeView === 'editor'" class="editor-wrapper">
            <MonacoEditor
              ref="monacoRef"
              v-model="content"
              @change="handleContentChange"
            />
          </div>
          <div v-else class="preview-wrapper">
            <A4Page :content="debouncedContent" />
          </div>
        </div>
      </div>
    </template>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted, onUnmounted, watch } from 'vue'
import { useRoute } from 'vue-router'
import MonacoEditor from '../components/MonacoEditor.vue'
import SplitPane from '../components/SplitPane.vue'
import A4Page from '../components/A4Page.vue'
import { GetResume } from '../wailsjs/go/main/App'
import { createDebounce } from '../composables/useDebounce'
import type { Resume } from '../types/resume'

const route = useRoute()
const monacoRef = ref<InstanceType<typeof MonacoEditor> | null>(null)

// 编辑器内容
const content = ref('')
const debouncedContent = ref('')

// 响应式状态 per D-09
const windowWidth = ref(window.innerWidth)
const isSinglePane = computed(() => windowWidth.value < 1200)
const activeView = ref<'editor' | 'preview'>('editor')

// 加载简历数据
onMounted(async () => {
  const resumeId = route.params.id as string
  if (resumeId) {
    try {
      const resume: Resume = await GetResume(resumeId)
      content.value = resume.markdownContent || ''
      debouncedContent.value = resume.markdownContent || ''
    } catch (err) {
      console.error('Failed to load resume:', err)
    }
  }

  // 监听窗口宽度变化
  window.addEventListener('resize', handleResize)
})

onUnmounted(() => {
  window.removeEventListener('resize', handleResize)
})

function handleResize() {
  windowWidth.value = window.innerWidth
}

// 内容变化处理 per D-10 (150ms debounce)
function handleContentChange(newContent: string) {
  content.value = newContent
  debouncedContent.value = newContent
}

// 简化方案：150ms 内更新 debouncedContent（满足 <200ms）
// 注意：如果需要更精确的防抖，可在 MonacoEditor.vue 中暴露 editor 实例后使用 createDebounce
watch(content, (newVal) => {
  // 150ms 后更新预览内容
  let timer: ReturnType<typeof setTimeout> | null = null
  if (timer) clearTimeout(timer)
  timer = setTimeout(() => {
    debouncedContent.value = newVal
  }, 150)
})

// 编辑器与预览滚动同步 per D-11
// 在 onMounted 中设置（需要等待 Monaco 实例就绪）
onMounted(() => {
  setTimeout(setupScrollSync, 500)
})

function setupScrollSync() {
  const editorInstance = monacoRef.value?.getEditor?.()
  if (!editorInstance) return

  const container = document.querySelector('.preview-container')
  if (!container) return

  editorInstance.onDidScrollChange((e: any) => {
    const scrollTop = e.scrollTop
    const scrollHeight = e.scrollHeight
    const clientHeight = e.height
    if (scrollHeight <= clientHeight) return

    const ratio = scrollTop / (scrollHeight - clientHeight)
    container.scrollTop = ratio * (container.scrollHeight - container.clientHeight)
  })
}
</script>

<style scoped>
.editor-view {
  height: 100%;
  width: 100%;
  overflow: hidden;
}

/* 双栏模式 */
.editor-pane,
.preview-pane {
  height: 100%;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.pane-header {
  height: 36px;
  min-height: 36px;
  padding: 0 12px;
  background: #252526;
  border-bottom: 1px solid #3c3c3c;
  display: flex;
  align-items: center;
}

.pane-title {
  font-size: 12px;
  color: #8b949e;
  font-weight: 500;
}

.editor-wrapper {
  flex: 1;
  overflow: hidden;
}

/* 单栏模式 per D-09 */
.single-pane-mode {
  height: 100%;
  display: flex;
  flex-direction: column;
}

.view-tabs {
  display: flex;
  height: 36px;
  min-height: 36px;
  background: #252526;
  border-bottom: 1px solid #3c3c3c;
}

.view-tabs button {
  flex: 1;
  background: transparent;
  border: none;
  color: #8b949e;
  font-size: 13px;
  cursor: pointer;
  transition: color 0.15s, background-color 0.15s;
}

.view-tabs button.active {
  color: #e0e0e0;
  background: #1e1e1e;
  border-bottom: 2px solid #0078d4;
}

.view-tabs button:hover:not(.active) {
  color: #e0e0e0;
  background: #2d2d2d;
}

.single-pane-content {
  flex: 1;
  overflow: hidden;
  position: relative;
}

.single-pane-content .editor-wrapper,
.single-pane-content .preview-wrapper {
  height: 100%;
  overflow: hidden;
}
</style>
```

**per D-09 (<1200px auto single-pane), D-10 (150ms debounce), D-11 (scroll sync), D-12 (error handling)**
</action>
<verify>
  <automated>cd /home/darven/桌面/dev_app/darvin-resume/frontend && npm run build 2>&1 | tail -10</automated>
</verify>
<done>EditorView.vue 编译通过，整合了所有 Phase 2 编辑器组件</done>
</task>

</tasks>

<verification>
- 窗口宽度 <1200px 时自动切换为单栏 tab 模式
- 编辑区切换到预览区后，视图正确切换
- 加载简历后 content 正确填充到 Monaco Editor
- 150ms 内预览区渲染更新（满足<200ms）
- 编辑器滚动时预览区同步滚动
</verification>

<success_criteria>
- 双栏模式下左侧为编辑器，右侧为 A4 预览，分割线可拖拽
- 单栏模式下通过 Tab 切换编辑/预览视图
- 内容变化后预览区在 150ms 内更新（<200ms per EDIT-06）
- 编辑器与预览区滚动同步
- 从 URL 参数加载简历内容
- 编辑器卸载时正确清理资源
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-editor/02-03-SUMMARY.md`
</output>
