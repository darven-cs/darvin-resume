# Plan 04-01: 简历列表页 — 卡片网格、排序、搜索、CRUD

**Phase:** 4 of 7 — 简历创建与管理
**Status:** Planned
**Depends on:** Phase 3 complete

## Goal

重构 HomeView 为卡片网格布局的简历列表页，支持排序、搜索、重命名、复制、删除操作，提供创建模式选择弹窗。

## Requirements Covered
- **RESM-05**: 简历创建入口 — AI引导/空白页两种模式选择弹窗
- **RESM-06**: 简历列表页 — 卡片网格、排序、搜索、重命名、复制、删除

## Deliverables

### 1. 后端新增方法 (`internal/service/resume.go`)

**新增 ResumeService 接口方法：**

```go
// RenameResume 修改简历标题
RenameResume(ctx context.Context, id string, title string) error

// DuplicateResume 复制简历（新记录 + "(副本)" 后缀）
DuplicateResume(ctx context.Context, id string) (*model.Resume, error)

// RestoreResume 恢复软删除（is_deleted = false, deleted_at = null）
RestoreResume(ctx context.Context, id string) error

// PermanentDeleteResume 物理删除记录
PermanentDeleteResume(ctx context.Context, id string) error

// ListDeletedResumes 查询已软删除的简历列表
ListDeletedResumes(ctx context.Context) ([]*model.ResumeListItem, error)
```

**SQL 查询：**
- `RenameResume`: `UPDATE resumes SET title=?, updated_at=? WHERE id=? AND is_deleted=FALSE`
- `DuplicateResume`: `SELECT * → INSERT 新记录 (id=新UUID, title=title+"(副本)")`
- `RestoreResume`: `UPDATE resumes SET is_deleted=FALSE, deleted_at=NULL, updated_at=? WHERE id=? AND is_deleted=TRUE`
- `PermanentDeleteResume`: `DELETE FROM resumes WHERE id=? AND is_deleted=TRUE`
- `ListDeletedResumes`: `SELECT id, title, updated_at FROM resumes WHERE is_deleted=TRUE ORDER BY deleted_at DESC`

### 2. Bridge 绑定 (`app.go`)

```go
func (a *App) RenameResume(id string, title string) error
func (a *App) DuplicateResume(id string) (*model.Resume, error)
func (a *App) RestoreResume(id string) error
func (a *App) PermanentDeleteResume(id string) error
func (a *App) ListDeletedResumes() ([]*model.ResumeListItem, error)
```

### 3. 前端类型扩展 (`frontend/src/types/resume.ts`)

```typescript
// ResumeListItem 增加 deletedAt 字段
export interface ResumeListItem {
  id: string
  title: string
  updatedAt: string
  deletedAt?: string  // 新增，回收站列表需要
}
```

### 4. 简历卡片组件 (`frontend/src/components/ResumeCard.vue`)

**Props:** `resume: ResumeListItem`
**功能：**
- 卡片主体显示：标题、最近修改时间（相对时间格式）、Markdown 内容渲染预览缩略图
- 缩略图：使用 mini A4Page 或截取前 3 行 Markdown 渲染内容的缩略快照，Phase 5 完善缓存优化
- hover 时右上角显示快捷操作按钮组（重命名/复制/删除）
- 点击卡片主体 → 路由跳转 `/editor/:id`
- 删除操作需二次确认（使用 `confirm()` 或自定义弹窗）
- 重命名单击后变为 inline input，回车确认，Escape 取消

**样式 per D-13~D-18:**
- 卡片网格布局，响应式排列（大屏 3~4 列，中屏 2 列，小屏 1 列）
- 使用 CSS Grid `grid-template-columns: repeat(auto-fill, minmax(280px, 1fr))`
- 卡片固定高度约 160px，`overflow: hidden`
- 圆角 8px，`border: 1px solid #3c3c3c`，hover 时边框高亮

### 5. 创建模式选择弹窗 (`frontend/src/components/CreateModeModal.vue`)

**Props:** `visible: boolean`
**Events:** `@close`, `@select-mode(mode: 'wizard' | 'blank')`

**功能 per D-01:**
- 居中模态弹窗，两个选项卡片：
  - 「AI 引导创建」— 图标 + 简短描述「分步填写，AI 润色优化」
  - 「空白页创建」— 图标 + 简短描述「自由编辑，随时调用 AI」
- 点击选项后 emit `select-mode` 事件并关闭弹窗
- 点击遮罩或右上角 X 按钮关闭

### 6. HomeView 重构 (`frontend/src/views/HomeView.vue`)

**功能 per D-13~D-18:**
- 顶部栏：左侧标题 "Darvin-Resume" + 右侧搜索框 + 「新建简历」按钮
- 搜索框支持按标题实时过滤（前端过滤，computed）
- 「新建简历」按钮点击 → 打开 `CreateModeModal`
- 空白页模式：调用 `CreateResume(title)` → 路由跳转 `/editor/:id`
- AI 引导模式：调用 `CreateResume(title)` → 路由跳转 `/editor/:id` + query param `?wizard=true`
- 卡片网格区域：使用 `ResumeCard` 组件渲染
- 排序按钮：按修改时间排序（默认最新在前，可切换最早在前）
- 空状态（无简历时）：居中显示引导文案 + 醒目「新建简历」按钮

### 7. 简历列表状态管理 (`frontend/src/composables/useResumeList.ts`)

```typescript
export function useResumeList() {
  // 响应式列表
  const resumes: Ref<ResumeListItem[]>
  const loading: Ref<boolean>
  const searchQuery: Ref<string>
  const sortOrder: Ref<'newest' | 'oldest'>

  // 过滤后列表 (computed)
  const filteredResumes: ComputedRef<ResumeListItem[]>

  // 操作方法
  function fetchResumes(): Promise<void>
  function renameResume(id: string, title: string): Promise<void>
  function duplicateResume(id: string): Promise<void>
  function deleteResume(id: string): Promise<void>

  return { resumes, loading, searchQuery, sortOrder, filteredResumes, fetchResumes, renameResume, duplicateResume, deleteResume }
}
```

## Technical Details

### 卡片网格 CSS

```css
.resume-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 16px;
  padding: 16px;
}
```

### 搜索过滤

使用前端 `computed` 过滤，不需要后端支持：
```typescript
const filteredResumes = computed(() => {
  let list = resumes.value
  if (searchQuery.value) {
    const query = searchQuery.value.toLowerCase()
    list = list.filter(r => r.title.toLowerCase().includes(query))
  }
  return list.sort((a, b) => {
    const dateA = new Date(a.updatedAt).getTime()
    const dateB = new Date(b.updatedAt).getTime()
    return sortOrder.value === 'newest' ? dateB - dateA : dateA - dateB
  })
})
```

### 重命名 inline 编辑

- 单击重命名按钮 → 标题变为 `<input>`
- `@keyup.enter` → 调用 `RenameResume(id, newTitle)`
- `@keyup.escape` → 恢复原值
- `@blur` → 自动确认（等同 enter）
- 输入框自动聚焦并全选

## Verification

- [ ] 简历列表正确加载并显示为卡片网格
- [ ] 搜索框可以按标题实时过滤
- [ ] 排序按钮在最新/最早之间正确切换
- [ ] 重命名操作正确更新标题并反映到卡片上
- [ ] 复制操作创建新简历，标题带 "(副本)" 后缀
- [ ] 删除操作软删除简历，卡片从列表消失
- [ ] 空状态正确显示引导文案和新建按钮
- [ ] CreateModeModal 正确弹窗并 emit 选择结果
- [ ] 后端新方法单元测试覆盖（Rename/Duplicate/Restore/PermanentDelete/ListDeleted）
- [ ] `npm run build` 无错误
