---
phase: "05-templates-export"
plan: "04"
type: "execute"
wave: "3"
depends_on:
  - "05-03"
files_modified:
  - "internal/database/migrations/004_create_snapshots_table.sql"
  - "internal/model/snapshot.go"
  - "internal/service/snapshot.go"
  - "app.go"
  - "frontend/src/components/SnapshotSidebar.vue"
  - "frontend/src/composables/useSnapshot.ts"
  - "frontend/src/views/EditorView.vue"
autonomous: true
requirements:
  - "EXPT-05"
  - "EXPT-06"
  - "EXPT-07"
  - "EXPT-08"
  - "EXPT-09"
user_setup: []

must_haves:
  truths:
    - "用户可以点击创建快照，输入标签和备注"
    - "PDF导出成功后自动创建一个快照（trigger_type='auto_pdf_export'）"
    - "用户可以在快照列表中查看所有历史快照"
    - "用户可以选择两个快照对比，查看 Markdown 内容差异"
    - "用户点击回滚后，当前内容被自动快照保存，然后恢复到选定版本"
    - "快照数量超过50个时，自动删除最旧的快照"
  artifacts:
    - path: "internal/database/migrations/004_create_snapshots_table.sql"
      provides: "snapshots 表定义"
      contains: "CREATE TABLE snapshots"
    - path: "internal/model/snapshot.go"
      provides: "Snapshot Go model"
      exports: ["Snapshot"]
    - path: "internal/service/snapshot.go"
      provides: "SnapshotService CRUD + Diff + Rollback"
      exports: ["CreateSnapshot", "ListSnapshots", "GetSnapshot", "DiffSnapshots", "RollbackToSnapshot", "DeleteSnapshot"]
    - path: "frontend/src/components/SnapshotSidebar.vue"
      provides: "快照侧边栏 UI"
      exports: ["snapshots", "createSnapshot", "rollback"]
    - path: "frontend/src/composables/useSnapshot.ts"
      provides: "快照状态管理 composable"
      exports: ["loadSnapshots", "createSnapshot", "diffSnapshots", "rollback"]
  key_links:
    - from: "frontend/src/components/ExportDialog.vue"
      to: "frontend/src/composables/useSnapshot.ts"
      via: "exported 事件触发自动快照"
      pattern: "exported.*createSnapshot"
    - from: "frontend/src/composables/useSnapshot.ts"
      to: "internal/service/snapshot.go"
      via: "wails bridge 调用"
      pattern: "App\\.(Create|List|Get|Diff|Rollback)Snapshot"
    - from: "frontend/src/components/SnapshotSidebar.vue"
      to: "frontend/src/components/AIDiffView.vue"
      via: "diff 对比视图复用"
      pattern: "AIDiffView|diff"
---

<objective>
实现版本快照管理：手动创建快照（标签+备注）、PDF导出后自动创建快照、版本历史列表、两版本 Diff 对比、一键回滚（回滚前自动快照当前）、快照存储完整数据（JSON+Markdown+模板+CSS）、50个上限自动清理。

**EXPT-05:** 手动创建快照（标签+备注）
**EXPT-06:** PDF导出后自动创建快照
**EXPT-07:** 版本列表 + 两版本 Diff 对比
**EXPT-08:** 一键回滚（回滚前自动快照当前）
**EXPT-09:** 快照完整存储 + 50个上限

Purpose: 保护用户的工作成果，支持版本历史管理和回退能力
Output: snapshots 表、SnapshotService、SnapshotSidebar、useSnapshot composable、Dif f视图集成
</objective>

<context>
@internal/model/resume.go
@internal/service/resume.go
@internal/database/db.go
@app.go
@frontend/src/components/AIDiffView.vue
@frontend/src/views/EditorView.vue
</context>

<interfaces>
<!-- 快照管理涉及的数据流 -->

From Snapshot model (创建中):
```go
type Snapshot struct {
    ID              string    `json:"id"`
    ResumeID        string    `json:"resumeId"`
    Label           string    `json:"label"`         // 用户自定义标签
    Note            string    `json:"note"`          // 用户备注
    TriggerType     string    `json:"triggerType"`    // 'manual'|'auto_pdf_export'|'rollback'
    JSONData        string    `json:"jsonData"`       // 完整简历 JSON
    MarkdownContent string    `json:"markdownContent"`  // Markdown 内容
    TemplateID      string    `json:"templateId"`     // 模板 ID
    CustomCSS       string    `json:"customCss"`      // 自定义 CSS
    CreatedAt       time.Time `json:"createdAt"`
}
```

From SnapshotService (创建中):
```go
type SnapshotService interface {
    CreateSnapshot(ctx context.Context, req *CreateSnapshotRequest) (*Snapshot, error)
    ListSnapshots(ctx context.Context, resumeId string) ([]*Snapshot, error)
    GetSnapshot(ctx context.Context, snapshotId string) (*Snapshot, error)
    DiffSnapshots(ctx context.Context, id1 string, id2 string) (*DiffResult, error)
    RollbackToSnapshot(ctx context.Context, resumeId string, snapshotId string) (*Snapshot, error)
    DeleteSnapshot(ctx context.Context, snapshotId string) error
    CleanupOldSnapshots(ctx context.Context, resumeId string, maxCount int) error
}

type CreateSnapshotRequest struct {
    ResumeID    string
    Label       string
    Note        string
    TriggerType string
}
```

From frontend/src/components/AIDiffView.vue (existing):
- 已用于 Phase 3 的 AI Diff 视图
- 复用同一组件展示快照 Markdown Diff
- 接收 `original` 和 `modified` 两个字符串 prop
- 渲染 side-by-side 对比视图

From frontend diff npm package:
```typescript
import * as Diff from 'diff'
// Diff.createPatch(file, oldStr, newStr, oldHeader, newHeader): string
// Diff.diffLines(oldStr, newStr): Change[]
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 snapshots 表迁移和 Snapshot model</name>
  <files>
    internal/database/migrations/004_create_snapshots_table.sql
    internal/model/snapshot.go
  </files>
  <action>
**internal/database/migrations/004_create_snapshots_table.sql：**

```sql
-- +goose Up
-- +goose StatementBegin
CREATE TABLE IF NOT EXISTS snapshots (
    id TEXT PRIMARY KEY,
    resume_id TEXT NOT NULL,
    label TEXT NOT NULL DEFAULT '',
    note TEXT NOT NULL DEFAULT '',
    trigger_type TEXT NOT NULL DEFAULT 'manual',
    json_data TEXT NOT NULL,
    markdown_content TEXT NOT NULL,
    template_id TEXT NOT NULL DEFAULT 'minimal',
    custom_css TEXT NOT NULL DEFAULT '',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (resume_id) REFERENCES resumes(id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_snapshots_resume_id ON snapshots(resume_id);
CREATE INDEX IF NOT EXISTS idx_snapshots_created_at ON snapshots(created_at);
-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin
DROP TABLE IF EXISTS snapshots;
-- +goose StatementEnd
```

**internal/model/snapshot.go：**

```go
package model

import "time"

// Snapshot represents a version snapshot of a resume
type Snapshot struct {
    ID              string    `json:"id"`
    ResumeID        string    `json:"resumeId"`
    Label           string    `json:"label"`
    Note            string    `json:"note"`
    TriggerType     string    `json:"triggerType"` // 'manual' | 'auto_pdf_export' | 'rollback'
    JSONData        string    `json:"jsonData"`
    MarkdownContent string    `json:"markdownContent"`
    TemplateID      string    `json:"templateId"`
    CustomCSS       string    `json:"customCss"`
    CreatedAt       time.Time `json:"createdAt"`
}

// CreateSnapshotRequest 用于创建快照的请求参数
type CreateSnapshotRequest struct {
    ResumeID    string `json:"resumeId"`
    Label       string `json:"label"`
    Note        string `json:"note"`
    TriggerType string `json:"triggerType"` // 'manual' | 'auto_pdf_export' | 'rollback'
}

// SnapshotListItem 用于列表展示（不含完整数据）
type SnapshotListItem struct {
    ID          string    `json:"id"`
    ResumeID    string    `json:"resumeId"`
    Label       string    `json:"label"`
    Note        string    `json:"note"`
    TriggerType string    `json:"triggerType"`
    CreatedAt   time.Time `json:"createdAt"`
}

// DiffResult 快照对比结果
type DiffResult struct {
    Snapshot1ID     string   `json:"snapshot1Id"`
    Snapshot2ID     string   `json:"snapshot2Id"`
    Snapshot1Label  string   `json:"snapshot1Label"`
    Snapshot2Label  string   `json:"snapshot2Label"`
    ContentDiff     string   `json:"contentDiff"`     // 完整 diff patch 字符串
    Stats           DiffStats `json:"stats"`
}

// DiffStats 对比统计
type DiffStats struct {
    AddedLines   int `json:"addedLines"`
    RemovedLines int `json:"removedLines"`
    ChangedLines int `json:"changedLines"`
}
```
</action>
  <verify>
cd /home/darven/桌面/dev_app/darvin-resume && go build ./internal/model/... 2>&1 | head -5
</verify>
  <done>
snapshots 迁移文件存在，Snapshot Go model 定义完整，包含 CreateSnapshotRequest、SnapshotListItem、DiffResult
</done>
</task>

<task type="auto">
  <name>Task 2: 创建 SnapshotService</name>
  <files>
    internal/service/snapshot.go
  </files>
  <action>
创建 `internal/service/snapshot.go`：

```go
package service

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
	"time"

	"Darvin-Resume/internal/database"
	"Darvin-Resume/internal/model"

	"github.com/google/uuid"
	"github.com/sergi/go-diff/diffmatchpatch"
)

var ErrSnapshotNotFound = errors.New("snapshot not found")
var ErrInvalidSnapshot = errors.New("invalid snapshot")

const MaxSnapshotsPerResume = 50

type SnapshotService interface {
	CreateSnapshot(ctx context.Context, req *model.CreateSnapshotRequest) (*model.Snapshot, error)
	ListSnapshots(ctx context.Context, resumeId string) ([]*model.SnapshotListItem, error)
	GetSnapshot(ctx context.Context, snapshotId string) (*model.Snapshot, error)
	DiffSnapshots(ctx context.Context, id1 string, id2 string) (*model.DiffResult, error)
	RollbackToSnapshot(ctx context.Context, resumeId string, snapshotId string) (*model.Snapshot, error)
	DeleteSnapshot(ctx context.Context, snapshotId string) error
	CleanupOldSnapshots(ctx context.Context, resumeId string, maxCount int) error
}

type snapshotService struct{}

// 引入 diffmatchpatch 用于文本对比
var dmp = diffmatchpatch.New()

func NewSnapshotService() SnapshotService {
	return &snapshotService{}
}

// CreateSnapshot 创建版本快照
func (s *snapshotService) CreateSnapshot(ctx context.Context, req *model.CreateSnapshotRequest) (*model.Snapshot, error) {
	if req.ResumeID == "" {
		return nil, errors.New("resume id is required")
	}

	// 获取当前简历的完整数据
	resume, err := s.getResume(ctx, req.ResumeID)
	if err != nil {
		return nil, err
	}

	snapshot := &model.Snapshot{
		ID:              uuid.New().String(),
		ResumeID:        req.ResumeID,
		Label:           req.Label,
		Note:            req.Note,
		TriggerType:     req.TriggerType,
		JSONData:        resume.JSONData,
		MarkdownContent: resume.MarkdownContent,
		TemplateID:      resume.TemplateID,
		CustomCSS:       resume.CustomCSS,
		CreatedAt:       time.Now(),
	}

	query := `
		INSERT INTO snapshots (id, resume_id, label, note, trigger_type, json_data, markdown_content, template_id, custom_css, created_at)
		VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
	`
	_, err = database.DB.ExecContext(ctx, query,
		snapshot.ID,
		snapshot.ResumeID,
		snapshot.Label,
		snapshot.Note,
		snapshot.TriggerType,
		snapshot.JSONData,
		snapshot.MarkdownContent,
		snapshot.TemplateID,
		snapshot.CustomCSS,
		snapshot.CreatedAt,
	)
	if err != nil {
		return nil, err
	}

	// 清理超出上限的旧快照
	if err := s.CleanupOldSnapshots(ctx, req.ResumeID, MaxSnapshotsPerResume); err != nil {
		// 清理失败不影响快照创建，仅记录日志
	}

	return snapshot, nil
}

// ListSnapshots 获取简历的所有快照（不含完整数据，仅列表信息）
func (s *snapshotService) ListSnapshots(ctx context.Context, resumeId string) ([]*model.SnapshotListItem, error) {
	query := `
		SELECT id, resume_id, label, note, trigger_type, created_at
		FROM snapshots
		WHERE resume_id = ?
		ORDER BY created_at DESC
	`
	rows, err := database.DB.QueryContext(ctx, query, resumeId)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var items []*model.SnapshotListItem
	for rows.Next() {
		item := &model.SnapshotListItem{}
		if err := rows.Scan(&item.ID, &item.ResumeID, &item.Label, &item.Note, &item.TriggerType, &item.CreatedAt); err != nil {
			return nil, err
		}
		items = append(items, item)
	}
	return items, rows.Err()
}

// GetSnapshot 获取单个快照的完整数据
func (s *snapshotService) GetSnapshot(ctx context.Context, snapshotId string) (*model.Snapshot, error) {
	query := `
		SELECT id, resume_id, label, note, trigger_type, json_data, markdown_content, template_id, custom_css, created_at
		FROM snapshots
		WHERE id = ?
	`
	row := database.DB.QueryRowContext(ctx, query, snapshotId)

	snapshot := &model.Snapshot{}
	err := row.Scan(
		&snapshot.ID,
		&snapshot.ResumeID,
		&snapshot.Label,
		&snapshot.Note,
		&snapshot.TriggerType,
		&snapshot.JSONData,
		&snapshot.MarkdownContent,
		&snapshot.TemplateID,
		&snapshot.CustomCSS,
		&snapshot.CreatedAt,
	)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, ErrSnapshotNotFound
		}
		return nil, err
	}
	return snapshot, nil
}

// DiffSnapshots 对比两个快照的内容差异
func (s *snapshotService) DiffSnapshots(ctx context.Context, id1 string, id2 string) (*model.DiffResult, error) {
	snap1, err := s.GetSnapshot(ctx, id1)
	if err != nil {
		return nil, err
	}
	snap2, err := s.GetSnapshot(ctx, id2)
	if err != nil {
		return nil, err
	}

	// 使用 diffmatchpatch 计算差异
	diffs := dmp.DiffMain(snap1.MarkdownContent, snap2.MarkdownContent, false)
	diffs = dmp.DiffCleanupSemantic(diffs)

	// 统计
	var added, removed, changed int
	for _, d := range diffs {
		switch d.Type {
		case diffmatchpatch.DiffInsert:
			added += strings.Count(d.Text, "\n") + 1
		case diffmatchpatch.DiffDelete:
			removed += strings.Count(d.Text, "\n") + 1
		}
	}

	// 生成 unified diff 格式
	diffStr := dmp.DiffToDelta(diffs)

	return &model.DiffResult{
		Snapshot1ID:    id1,
		Snapshot2ID:    id2,
		Snapshot1Label: snap1.Label,
		Snapshot2Label: snap2.Label,
		ContentDiff:    diffStr,
		Stats: model.DiffStats{
			AddedLines:   added,
			RemovedLines: removed,
			ChangedLines: changed,
		},
	}, nil
}

// RollbackToSnapshot 回滚到指定快照
// 回滚前自动创建当前状态的快照（trigger_type = 'rollback'）
func (s *snapshotService) RollbackToSnapshot(ctx context.Context, resumeId string, snapshotId string) (*model.Snapshot, error) {
	// 1. 获取目标快照
	targetSnap, err := s.GetSnapshot(ctx, snapshotId)
	if err != nil {
		return nil, err
	}

	// 2. 回滚前自动创建当前快照
	autoSnap, err := s.CreateSnapshot(ctx, &model.CreateSnapshotRequest{
		ResumeID:    resumeId,
		Label:       fmt.Sprintf("回滚至「%s」前自动备份", targetSnap.Label),
		Note:        "系统自动创建的回滚前备份",
		TriggerType: "rollback",
	})
	if err != nil {
		// 回滚前备份失败不影响回滚操作，但记录错误
	}

	// 3. 更新简历数据
	query := `
		UPDATE resumes
		SET json_data = ?, markdown_content = ?, template_id = ?, custom_css = ?, updated_at = ?
		WHERE id = ? AND is_deleted = FALSE
	`
	now := time.Now()
	_, err = database.DB.ExecContext(ctx, query,
		targetSnap.JSONData,
		targetSnap.MarkdownContent,
		targetSnap.TemplateID,
		targetSnap.CustomCSS,
		now,
		resumeId,
	)
	if err != nil {
		return nil, err
	}

	return autoSnap, nil
}

// DeleteSnapshot 删除快照
func (s *snapshotService) DeleteSnapshot(ctx context.Context, snapshotId string) error {
	query := `DELETE FROM snapshots WHERE id = ?`
	result, err := database.DB.ExecContext(ctx, query, snapshotId)
	if err != nil {
		return err
	}
	rows, err := result.RowsAffected()
	if err != nil {
		return err
	}
	if rows == 0 {
		return ErrSnapshotNotFound
	}
	return nil
}

// CleanupOldSnapshots 清理超出上限的旧快照
// 保留最新的 maxCount 个快照，删除更旧的
func (s *snapshotService) CleanupOldSnapshots(ctx context.Context, resumeId string, maxCount int) error {
	// 检查当前快照数量
	countQuery := `SELECT COUNT(*) FROM snapshots WHERE resume_id = ?`
	var count int
	if err := database.DB.QueryRowContext(ctx, countQuery, resumeId).Scan(&count); err != nil {
		return err
	}

	if count <= maxCount {
		return nil
	}

	// 删除超出上限的旧快照
	deleteCount := count - maxCount
	deleteQuery := `
		DELETE FROM snapshots
		WHERE id IN (
			SELECT id FROM snapshots
			WHERE resume_id = ?
			ORDER BY created_at ASC
			LIMIT ?
		)
	`
	_, err := database.DB.ExecContext(ctx, deleteQuery, resumeId, deleteCount)
	return err
}

// getResume 获取简历数据（内部辅助方法）
func (s *snapshotService) getResume(ctx context.Context, resumeId string) (*snapshotData, error) {
	type snapshotData struct {
		JSONData        string
		MarkdownContent string
		TemplateID      string
		CustomCSS       string
	}
	query := `
		SELECT json_data, markdown_content, template_id, custom_css
		FROM resumes
		WHERE id = ? AND is_deleted = FALSE
	`
	row := database.DB.QueryRowContext(ctx, query, resumeId)
	data := &snapshotData{}
	err := row.Scan(&data.JSONData, &data.MarkdownContent, &data.TemplateID, &data.CustomCSS)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, ErrResumeNotFound
		}
		return nil, err
	}
	return data, nil
}
```

**注意：** 需要在 go.mod 中添加 `github.com/sergi/go-diff/diffmatchpatch` 用于文本 Diff。
</action>
  <verify>
cd /home/darven/桌面/dev_app/darvin-resume && go build ./internal/service/... 2>&1 | head -10
</verify>
  <done>
SnapshotService 实现完整，包含 Create/List/Get/Diff/Rollback/Delete/CleanupOldSnapshots 方法
</done>
</task>

<task type="auto">
  <name>Task 3: 添加 Snapshot bridge 方法到 app.go</name>
  <files>
    app.go
  </files>
  <action>
在 `app.go` 中添加 Snapshot 相关的 Wails bridge 方法：

```go
// ============================================================
// Snapshot Bridge Methods (EXPT-05 ~ EXPT-09)
// ============================================================

// CreateSnapshot 创建版本快照
func (a *App) CreateSnapshot(resumeId string, label string, note string, triggerType string) (*model.Snapshot, error) {
	svc := service.NewSnapshotService()
	req := &model.CreateSnapshotRequest{
		ResumeID:    resumeId,
		Label:       label,
		Note:        note,
		TriggerType: triggerType,
	}
	return svc.CreateSnapshot(context.Background(), req)
}

// ListSnapshots 获取简历的所有快照列表
func (a *App) ListSnapshots(resumeId string) ([]*model.SnapshotListItem, error) {
	svc := service.NewSnapshotService()
	return svc.ListSnapshots(context.Background(), resumeId)
}

// GetSnapshot 获取单个快照的完整数据
func (a *App) GetSnapshot(snapshotId string) (*model.Snapshot, error) {
	svc := service.NewSnapshotService()
	return svc.GetSnapshot(context.Background(), snapshotId)
}

// DiffSnapshots 对比两个快照
func (a *App) DiffSnapshots(id1 string, id2 string) (*model.DiffResult, error) {
	svc := service.NewSnapshotService()
	return svc.DiffSnapshots(context.Background(), id1, id2)
}

// RollbackToSnapshot 回滚到指定快照
func (a *App) RollbackToSnapshot(resumeId string, snapshotId string) (*model.Snapshot, error) {
	svc := service.NewSnapshotService()
	return svc.RollbackToSnapshot(context.Background(), resumeId, snapshotId)
}

// DeleteSnapshot 删除快照
func (a *App) DeleteSnapshot(snapshotId string) error {
	svc := service.NewSnapshotService()
	return svc.DeleteSnapshot(context.Background(), snapshotId)
}

// GetSnapshotMarkdown 获取快照的 Markdown 内容（用于编辑器加载）
func (a *App) GetSnapshotMarkdown(snapshotId string) (string, string, string, string, error) {
	svc := service.NewSnapshotService()
	snap, err := svc.GetSnapshot(context.Background(), snapshotId)
	if err != nil {
		return "", "", "", "", err
	}
	return snap.MarkdownContent, snap.TemplateID, snap.CustomCSS, snap.JSONData, nil
}
```
</action>
  <verify>
cd /home/darven/桌面/dev_app/darvin-resume && go build ./... 2>&1 | head -20
</verify>
  <done>
后端编译通过，所有 Snapshot bridge 方法暴露
</done>
</task>

<task type="auto">
  <name>Task 4: 创建 useSnapshot.ts composable</name>
  <files>
    frontend/src/composables/useSnapshot.ts
  </files>
  <action>
创建 `frontend/src/composables/useSnapshot.ts`：

```typescript
import { ref } from 'vue'
import type { Snapshot, SnapshotListItem, DiffResult } from '@/types/snapshot'

export function useSnapshot() {
  const snapshots = ref<SnapshotListItem[]>([])
  const isLoading = ref(false)
  const diffResult = ref<DiffResult | null>(null)

  // 加载快照列表
  async function loadSnapshots(resumeId: string) {
    isLoading.value = true
    try {
      const list = await window.go.main.App.ListSnapshots(resumeId)
      snapshots.value = list || []
    } finally {
      isLoading.value = false
    }
  }

  // 创建快照
  async function createSnapshot(
    resumeId: string,
    label: string,
    note: string = '',
    triggerType: 'manual' = 'manual'
  ): Promise<Snapshot> {
    const snap = await window.go.main.App.CreateSnapshot(resumeId, label, note, triggerType)
    // 刷新列表
    await loadSnapshots(resumeId)
    return snap
  }

  // 自动创建快照（PDF导出后）
  async function autoCreateSnapshot(
    resumeId: string,
    label: string = 'PDF 导出快照',
    note: string = ''
  ): Promise<Snapshot> {
    return createSnapshot(resumeId, label, note, 'auto_pdf_export')
  }

  // 对比两个快照
  async function diffSnapshots(id1: string, id2: string): Promise<DiffResult> {
    diffResult.value = await window.go.main.App.DiffSnapshots(id1, id2)
    return diffResult.value
  }

  // 回滚到指定快照
  async function rollback(resumeId: string, snapshotId: string): Promise<Snapshot> {
    const snap = await window.go.main.App.RollbackToSnapshot(resumeId, snapshotId)
    // 刷新列表
    await loadSnapshots(resumeId)
    return snap
  }

  // 删除快照
  async function deleteSnapshot(snapshotId: string, resumeId: string) {
    await window.go.main.App.DeleteSnapshot(snapshotId)
    // 刷新列表
    await loadSnapshots(resumeId)
  }

  // 获取快照详情（用于回滚时预览）
  async function getSnapshot(snapshotId: string): Promise<Snapshot> {
    return await window.go.main.App.GetSnapshot(snapshotId)
  }

  // 获取快照 Markdown 内容（用于编辑器加载）
  async function getSnapshotMarkdown(snapshotId: string): Promise<{
    markdown: string
    templateId: string
    customCss: string
    jsonData: string
  }> {
    const [markdown, templateId, customCss, jsonData] = await window.go.main.App.GetSnapshotMarkdown(snapshotId)
    return { markdown, templateId, customCss, jsonData }
  }

  return {
    snapshots,
    isLoading,
    diffResult,
    loadSnapshots,
    createSnapshot,
    autoCreateSnapshot,
    diffSnapshots,
    rollback,
    deleteSnapshot,
    getSnapshot,
    getSnapshotMarkdown,
  }
}
```

**frontend/src/types/snapshot.ts（新建）：**

```typescript
export interface Snapshot {
  id: string
  resumeId: string
  label: string
  note: string
  triggerType: 'manual' | 'auto_pdf_export' | 'rollback'
  jsonData: string
  markdownContent: string
  templateId: string
  customCss: string
  createdAt: string
}

export interface SnapshotListItem {
  id: string
  resumeId: string
  label: string
  note: string
  triggerType: string
  createdAt: string
}

export interface DiffResult {
  snapshot1Id: string
  snapshot2Id: string
  snapshot1Label: string
  snapshot2Label: string
  contentDiff: string  // diffmatchpatch delta 格式
  stats: {
    addedLines: number
    removedLines: number
    changedLines: number
  }
}
```
</action>
  <verify>
cat frontend/src/composables/useSnapshot.ts | head -20
</verify>
  <done>
useSnapshot composable 导出 loadSnapshots, createSnapshot, autoCreateSnapshot, diffSnapshots, rollback, deleteSnapshot 方法
</done>
</task>

<task type="auto">
  <name>Task 5: 创建 SnapshotSidebar.vue 组件</name>
  <files>
    frontend/src/components/SnapshotSidebar.vue
  </files>
  <action>
创建 `frontend/src/components/SnapshotSidebar.vue`：

**侧边栏设计（右侧滑出面板）：**

**顶部：**
- 标题「版本快照」
- 「创建快照」按钮（打开创建对话框）

**快照列表：**
- 按时间倒序显示，每条包含：
  - 标签名称（加粗）+ 触发类型标签（manual/auto/rollback）
  - 备注（斜体，小字）
  - 创建时间（相对时间，如"3天前"或具体日期）
  - 操作按钮：查看 / 对比 / 回滚 / 删除
- 空状态：无快照时显示引导文案

**创建快照对话框：**
- 标签输入框（必填，最大20字符）
- 备注输入框（可选，最大100字符）
- 确定/取消按钮

**对比视图：**
- 选择两个快照（复选框或点击）
- 点击「对比」后，显示 AIDiffView 组件（复用 Phase 3 的 Diff 视图）
- 使用 diff npm 包渲染 side-by-side 对比
- 展示增加行数/删除行数统计

**回滚确认：**
- 点击回滚按钮后弹出确认框
- 显示「回滚后将自动创建当前版本备份」
- 确认后调用 rollback()，完成后刷新编辑器内容

**布局：**
- 面板宽度 360px，右侧滑出
- 快照列表使用 `<ul>` + `<li>` 紧凑布局
- 触发类型使用彩色标签（manual=蓝色，auto_pdf_export=绿色，rollback=橙色）
</action>
  <verify>
vue-tsc --noEmit frontend/src/components/SnapshotSidebar.vue 2>&1 | head -5
</verify>
  <done>
SnapshotSidebar.vue 存在，包含快照列表、创建对话框、对比视图、回滚确认
</done>
</task>

<task type="auto">
  <name>Task 6: 集成导出自动快照到 EditorView</name>
  <files>
    frontend/src/views/EditorView.vue
  </files>
  <action>
修改 `frontend/src/views/EditorView.vue`，集成快照相关功能：

**1. 引入 useSnapshot composable：**
```typescript
import { useSnapshot } from '@/composables/useSnapshot'
```

**2. 在 EditorView 中调用 useSnapshot：**
```typescript
const {
  snapshots,
  loadSnapshots,
  createSnapshot,
  autoCreateSnapshot,
  rollback,
} = useSnapshot()
```

**3. 在 onMounted 中加载快照列表：**
```typescript
onMounted(async () => {
  // ... Phase 4 的现有逻辑
  await loadSnapshots(resumeId)
})
```

**4. 集成 ExportDialog 的 exported 事件：**
```html
<ExportDialog
  :resume-id="resumeId"
  @exported="handleExportCompleted"
/>
```

```typescript
async function handleExportCompleted() {
  // PDF 导出成功后自动创建快照
  await autoCreateSnapshot(resumeId, `PDF 导出快照 ${new Date().toLocaleDateString()}`, '')
  toast.success('PDF 导出成功，已自动创建版本快照')
}
```

**5. 添加快照侧边栏入口按钮（编辑器工具栏）：**
在编辑器工具栏中添加「版本快照」按钮，点击打开 SnapshotSidebar。

```html
<button class="toolbar-btn" @click="showSnapshotSidebar = true" title="版本快照">
  <IconSnapshot />
</button>

<SnapshotSidebar
  v-if="showSnapshotSidebar"
  :resume-id="resumeId"
  @rollback="handleRollback"
  @close="showSnapshotSidebar = false"
/>
```

**6. 处理回滚：**
```typescript
async function handleRollback(snapshotId: string) {
  const snap = await rollback(resumeId, snapshotId)
  // 重新加载简历数据
  await loadResume(resumeId)
  toast.success('已回滚到指定版本')
  showSnapshotSidebar.value = false
}
```

**7. 添加创建快照入口（工具栏按钮）：**
```html
<button class="toolbar-btn" @click="showCreateSnapshotDialog = true" title="创建快照">
  <IconCamera />
</button>
```
</action>
  <verify>
grep -c "useSnapshot\|SnapshotSidebar\|autoCreateSnapshot" frontend/src/views/EditorView.vue
</verify>
  <done>
EditorView.vue 集成快照相关功能：工具栏按钮、SnapshotSidebar、导出自动快照 hook、回滚处理
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend -> backend (snapshot) | 快照数据通过 bridge 传递，无敏感操作 |
| rollback operation | 回滚会修改简历数据，但自动创建备份快照保护数据 |
| snapshot deletion | 删除快照不可逆，但数据在回滚快照中有备份 |
| snapshot storage | 快照存储在用户本地 SQLite，无云端泄露风险 |
</boundary>

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Denial of Service | Snapshot storage growth | mitigate | MaxSnapshotsPerResume = 50，超出自动清理最旧的 |
| T-05-09 | Data Loss | Rollback without backup | mitigate | 回滚前自动创建 rollback 类型快照，保证可回退 |
| T-05-10 | Integrity | Snapshot data corruption | accept | SQLite WAL 模式保证数据完整性，快照本身为只读历史 |
</threat_model>

<verification>
1. snapshots 表迁移文件存在且语法正确
2. SnapshotService 编译通过
3. 所有 Snapshot bridge 方法暴露且编译通过
4. useSnapshot composable 方法完整
5. SnapshotSidebar.vue 渲染快照列表
6. EditorView.vue 集成导出自动快照和回滚功能
</verification>

<success_criteria>
- [ ] snapshots 表迁移文件存在，包含所有必需字段
- [ ] SnapshotService 实现 Create/List/Get/Diff/Rollback/Delete/CleanupOldSnapshots
- [ ] 所有 Snapshot bridge 方法暴露且后端编译通过
- [ ] useSnapshot composable 提供 loadSnapshots, createSnapshot, autoCreateSnapshot, diffSnapshots, rollback, deleteSnapshot
- [ ] SnapshotSidebar.vue 展示快照列表，支持创建/对比/回滚/删除
- [ ] PDF 导出成功后自动触发 autoCreateSnapshot
- [ ] 回滚前自动创建 rollback 类型快照
- [ ] 快照数量超过 50 个时自动删除最旧的
- [ ] EditorView.vue 集成快照侧边栏入口
</success_criteria>

<output>
完成后创建 `.planning/phases/05-templates-export/05-04-SUMMARY.md`
</output>
