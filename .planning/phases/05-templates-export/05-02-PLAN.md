---
phase: "05-templates-export"
plan: "02"
type: "execute"
wave: "2"
depends_on:
  - "05-01"
files_modified:
  - "frontend/src/components/StyleEditor.vue"
  - "frontend/src/utils/sanitizeCSS.ts"
  - "frontend/src/styles/editor.css"
  - "frontend/src/composables/useTemplate.ts"
  - "frontend/src/views/EditorView.vue"
autonomous: true
requirements:
  - "TMPL-04"
  - "TMPL-05"
  - "TMPL-06"
user_setup:
  - "npm install dompurify postcss postcss-selector-parser"

must_haves:
  truths:
    - "用户拖动滑块调整字号后，预览区文字大小立即变化"
    - "用户选择主色调后，预览区标题和强调色立即变化"
    - "用户输入自定义 CSS 后，经过白名单校验才显示效果"
    - "用户点击一键重置，预览区样式恢复到当前模板默认值"
  artifacts:
    - path: "frontend/src/components/StyleEditor.vue"
      provides: "样式调整面板 UI（滑块/选择器）"
      exports: ["primaryColor", "fontSize", "lineHeight", "pagePadding", "fontFamily"]
    - path: "frontend/src/utils/sanitizeCSS.ts"
      provides: "CSS 白名单校验工具函数"
      exports: ["sanitizeCustomCSS", "DEFAULT_CSS_VARS"]
    - path: "frontend/src/styles/editor.css"
      provides: "CSS 变量定义（--resume-*）"
      contains: "--resume-primary-color"
  key_links:
    - from: "frontend/src/components/StyleEditor.vue"
      to: "frontend/src/composables/useTemplate.ts"
      via: "saveAsTemplate() 调用"
      pattern: "saveAsTemplate"
    - from: "frontend/src/utils/sanitizeCSS.ts"
      to: "frontend/src/components/StyleEditor.vue"
      via: "import sanitizeCustomCSS"
      pattern: "sanitizeCustomCSS"
    - from: "StyleEditor.vue"
      to: "frontend/src/components/A4Page.vue"
      via: "useTemplate 提供 customCss"
      pattern: "customCss"
---

<objective>
实现可视化样式调整（TMPL-04）、自定义 CSS 白名单安全机制（TMPL-05）、一键重置默认样式（TMPL-06）。

**TMPL-04:** 滑块/选择器调整主色调、字号、行高、边距、字体
**TMPL-05:** 自定义 CSS 白名单安全机制（DOMPurify + PostCSS）
**TMPL-06:** 一键重置默认样式

Purpose: 让用户在不写代码的情况下精确控制简历视觉效果，同时保证安全性
Output: StyleEditor 组件、sanitizeCSS 工具、CSS 变量系统、useTemplate 扩展
</objective>

<context>
@frontend/src/styles/editor.css
@frontend/src/components/A4Page.vue
@frontend/src/composables/useTemplate.ts
@frontend/src/components/TemplateSelector.vue
</context>

<interfaces>
<!-- 样式调整涉及的数据流 -->

From useTemplate.ts (from 05-01):
```typescript
// 模板样式参数
interface TemplateStyle {
  primaryColor: string      // '#1a1a1a'
  fontSize: number          // 10.5
  lineHeight: number        // 1.6
  pagePadding: number       // 20 (mm)
  fontFamily: string        // 'system-ui, sans-serif'
}

// 暴露的 API
function saveAsTemplate(css: string): Promise<void>
function loadTemplate(): Promise<void>
const customCss: Ref<string>
```

From sanitizeCSS.ts (本 plan 创建):
```typescript
export const DEFAULT_CSS_VARS: Record<string, string> = {
  '--resume-primary-color': '#1a1a1a',
  '--resume-font-size': '10.5pt',
  '--resume-line-height': '1.6',
  '--resume-padding': '20mm',
  '--resume-font-family': '-apple-system, BlinkMacSystemFont, sans-serif',
  '--resume-link-color': '#0066cc',
  '--resume-heading-color': '#1a1a1a',
}

export function sanitizeCustomCSS(input: string): string {
  // 返回白名单校验后的 CSS（危险属性/值被移除）
}
```

From editor.css (需追加 CSS 变量):
```css
:root {
  --resume-primary-color: #1a1a1a;
  --resume-font-size: 10.5pt;
  --resume-line-height: 1.6;
  --resume-padding: 20mm;
  --resume-font-family: -apple-system, BlinkMacSystemFont, sans-serif;
  --resume-link-color: #0066cc;
  --resume-heading-color: #1a1a1a;
  --resume-bg-color: #ffffff;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 在 editor.css 中定义 CSS 变量系统</name>
  <files>
    frontend/src/styles/editor.css
  </files>
  <action>
在 `frontend/src/styles/editor.css` 的 `:root` 块中追加 CSS 变量定义。这些变量将在 StyleEditor.vue 中被滑块/选择器修改：

在现有 `:root` 块中追加：
```css
/* =========================================
   模板样式变量（TMPL-04 可视化调整用）
   通过滑块/选择器修改这些变量值
   ========================================= */
:root {
  /* 主色调 */
  --resume-primary-color: #1a1a1a;
  --resume-heading-color: #1a1a1a;
  --resume-link-color: #0066cc;
  --resume-bg-color: #ffffff;

  /* 字号（默认 10.5pt = 14px） */
  --resume-font-size: 10.5pt;

  /* 行高（默认 1.6） */
  --resume-line-height: 1.6;

  /* 页面边距（默认 20mm） */
  --resume-padding: 20mm;

  /* 字体 */
  --resume-font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif;
}

/* 模板样式应用到 .page-content */
.page-content {
  color: var(--resume-primary-color);
  font-size: var(--resume-font-size);
  line-height: var(--resume-line-height);
  font-family: var(--resume-font-family);
}

.page-content h1,
.page-content h2,
.page-content h3,
.page-content h4 {
  color: var(--resume-heading-color);
}

.page-content a {
  color: var(--resume-link-color);
}

/* 背景色（用于有背景色的模板） */
.a4-page {
  background-color: var(--resume-bg-color);
  padding: var(--resume-padding);
}
```

确保现有的 `.page-content` 样式已使用这些变量（或被覆盖）。如果需要，修改现有样式使其引用 CSS 变量。
</action>
  <verify>
grep -c "\-\-resume-" frontend/src/styles/editor.css
</verify>
  <done>
editor.css 包含 --resume-primary-color、--resume-font-size、--resume-line-height、--resume-padding、--resume-font-family 变量定义，.page-content 引用这些变量
</done>
</task>

<task type="auto">
  <name>Task 2: 创建 CSS 白名单校验工具 sanitizeCSS.ts</name>
  <files>
    frontend/src/utils/sanitizeCSS.ts
  </files>
  <action>
创建 `frontend/src/utils/sanitizeCSS.ts`：

**白名单属性（30+ 安全属性）：**
```typescript
import postcss from 'postcss'
import type { Root } from 'postcss'

// 白名单属性（仅允许布局、排版、颜色、间距）
const ALLOWED_CSS_PROPERTIES = new Set([
  // 字体与文本
  'color', 'font', 'font-family', 'font-size', 'font-weight', 'font-style',
  'text-align', 'text-decoration', 'text-indent', 'text-transform',
  'letter-spacing', 'line-height', 'word-spacing',
  // 间距
  'margin', 'margin-top', 'margin-right', 'margin-bottom', 'margin-left',
  'padding', 'padding-top', 'padding-right', 'padding-bottom', 'padding-left',
  // 布局
  'display', 'flex-direction', 'justify-content', 'align-items', 'flex-wrap',
  'flex', 'flex-grow', 'flex-shrink', 'flex-basis',
  'column-count', 'column-gap', 'column-rule',
  'gap',
  // 尺寸
  'width', 'min-width', 'max-width', 'height', 'min-height', 'max-height',
  // 边框
  'border', 'border-width', 'border-style', 'border-color',
  'border-top', 'border-bottom', 'border-left', 'border-right',
  'border-radius', 'border-top-width', 'border-bottom-width',
  // 背景
  'background-color', 'background',
  // 列表
  'list-style', 'list-style-type',
  // 溢出
  'overflow', 'overflow-x', 'overflow-y',
  // 断行
  'break-inside', 'page-break-inside', '-webkit-column-break-inside',
  // 垂直对齐
  'vertical-align',
  // 空白
  'white-space',
])

// 危险属性（拒绝全部）
const DANGEROUS_PATTERNS = [
  /url\s*\(/i,              // 外部资源
  /expression\s*\(/i,        // CSS 表达式
  /javascript\s*:/i,         // JS 伪协议
  /@import/i,               // 外部样式表
  /@keyframes/i,            // 动画
  /@supports/i,             // 特性查询
  /@media/i,                // 媒体查询（防止逃逸）
  /--\w+:.*url/i,           // CSS 变量中的 url
  /data\s*:/i,              // data: 协议
]

// 允许的选择器前缀（防止选择器逃逸到页面任意元素）
const ALLOWED_SELECTOR_PREFIXES = [
  '.page-content',
  '.page-content ',
]

export const DEFAULT_CSS_VARS: Record<string, string> = {
  '--resume-primary-color': '#1a1a1a',
  '--resume-heading-color': '#1a1a1a',
  '--resume-link-color': '#0066cc',
  '--resume-bg-color': '#ffffff',
  '--resume-font-size': '10.5pt',
  '--resume-line-height': '1.6',
  '--resume-padding': '20mm',
  '--resume-font-family': '-apple-system, BlinkMacSystemFont, sans-serif',
}

export function sanitizeCustomCSS(input: string): string {
  if (!input || typeof input !== 'string') {
    return ''
  }

  try {
    const root = postcss.parse(input)

    // 移除所有 @ 规则（@import, @keyframes, @media, @supports）
    root.walkAtRules((rule) => {
      rule.remove()
    })

    // 遍历所有声明
    root.walkDecls((decl) => {
      const prop = decl.prop.toLowerCase()
      const value = decl.value

      // 1. 属性名白名单检查
      if (!ALLOWED_CSS_PROPERTIES.has(prop)) {
        decl.remove()
        return
      }

      // 2. 危险值模式检查
      for (const pattern of DANGEROUS_PATTERNS) {
        if (pattern.test(value)) {
          decl.remove()
          return
        }
      }

      // 3. 选择器前缀检查（如果不在允许的前缀内，拒绝）
      const parent = decl.parent
      if (parent && parent.type === 'rule') {
        const selector = parent.selector
        const isAllowed = ALLOWED_SELECTOR_PREFIXES.some(prefix =>
          selector === prefix || selector.startsWith(prefix)
        )
        if (!isAllowed) {
          decl.remove()
        }
      }
    })

    return root.toString()
  } catch {
    // 解析失败时返回空字符串（完全拒绝）
    return ''
  }
}

// 将样式参数转换为 CSS 变量声明
export function buildCSSVars(vars: Partial<typeof DEFAULT_CSS_VARS>): string {
  const entries = Object.entries(vars)
  if (entries.length === 0) return ''
  return `:root {\n${entries.map(([k, v]) => `  ${k}: ${v};`).join('\n')}\n}`
}
```

**npm 安装检查：** 确保 dompurify、postcss、postcss-selector-parser 已安装。
</action>
  <verify>
npx tsc --noEmit frontend/src/utils/sanitizeCSS.ts 2>&1 | head -10
</verify>
  <done>
sanitizeCSS.ts 存在，导出 sanitizeCustomCSS 和 buildCSSVars 函数，白名单包含 30+ 属性
</done>
</task>

<task type="auto">
  <name>Task 3: 创建 StyleEditor.vue 组件</name>
  <files>
    frontend/src/components/StyleEditor.vue
  </files>
  <action>
创建 `frontend/src/components/StyleEditor.vue`：

**组件设计（编辑器工具栏下拉面板或侧边栏抽屉）：**

通过 prop 接收 `resumeId` 和 `templateId`，注入 `useTemplate`。

**样式调整控件（TMPL-04）：**

1. **主色调选择器：** `<input type="color">` 颜色选择器 + 预设色板（#1a1a1a, #333, #0066cc, #2c5f8a, #5c3317, #1a4d1a），点击预设快速切换
2. **字号滑块：** `<input type="range">` 范围 8pt~14pt，默认 10.5pt，显示当前值
3. **行高滑块：** `<input type="range">` 范围 1.2~2.0，默认 1.6，步进 0.1，显示当前值
4. **页面边距滑块：** `<input type="range">` 范围 15mm~30mm，默认 20mm，步进 1mm
5. **字体选择器：** `<select>` 下拉选择系统字体栈
   - 选项：系统默认 / 微软雅黑 / 宋体 / 黑体 / Georgia（英文）/ Times New Roman（英文）
6. **实时预览：** 每个控件的值变更后立即更新 CSS 变量（不保存），预览区实时反映

**自定义 CSS 区域（TMPL-05）：**
- `<textarea>` 输入自定义 CSS 代码（限制 2000 字符）
- 实时显示校验结果：绿色勾号表示通过，红色 X 表示有危险属性被移除
- 底部显示「已移除 X 条危险规则」提示
- 输入内容经过 `sanitizeCustomCSS()` 校验后实时预览

**一键重置（TMPL-06）：**
- 「重置默认样式」按钮，点击后恢复当前模板的默认值
- 重置前弹窗确认（"确定要重置为默认样式吗？当前自定义样式将丢失"）
- 调用 `buildCSSVars(DEFAULT_CSS_VARS)` 恢复

**保存机制：**
- 所有样式调整通过 `saveAsTemplate(css)` 保存到后端
- 保存时将 CSS 变量 + 自定义 CSS 合并为一个字符串
- 格式：`/* CSS Variables */ :root { --resume-*: ...; } /* Custom CSS */ ...`

**布局：**
- 使用 Vue `<script setup>` + TypeScript
- 面板内分区：样式调整区 / 自定义 CSS 区 / 底部按钮区
- 响应式：小屏时控件堆叠排列
</action>
  <verify>
vue-tsc --noEmit frontend/src/components/StyleEditor.vue 2>&1 | head -5
</verify>
  <done>
StyleEditor.vue 存在，包含所有6种样式调整控件，实时预览，CSS白名单校验，一键重置按钮
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> CSS injection | 用户输入的 CSS 通过 sanitizeCustomCSS 过滤后注入到 DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Injection | StyleEditor.vue | mitigate | sanitizeCustomCSS 白名单拒绝所有非安全属性和危险模式（url, expression, @import, @keyframes） |
| T-05-02 | Tampering | CSS variable manipulation | accept | CSS 变量仅影响简历预览样式，无用户数据或系统配置修改能力 |
| T-05-03 | Information Disclosure | Custom CSS display | accept | 用户自定义 CSS 仅存储在用户自己的 SQLite 数据库中 |
</threat_model>

<verification>
1. editor.css 包含 --resume-* CSS 变量定义
2. sanitizeCSS.ts 白名单通过 postcss 解析测试
3. StyleEditor.vue 包含6种控件（主色调/字号/行高/边距/字体/自定义CSS）
4. 一键重置按钮存在且功能正确
5. 编译无错误
</verification>

<success_criteria>
- [ ] editor.css 定义了 --resume-primary-color、--resume-font-size、--resume-line-height、--resume-padding、--resume-font-family 变量
- [ ] sanitizeCSS.ts 白名单包含 30+ 安全属性，拒绝 url/expression/@import/@keyframes 等危险模式
- [ ] StyleEditor.vue 包含6种样式调整控件（主色调/字号/行高/边距/字体/自定义CSS）
- [ ] 样式调整控件变更实时反映到预览区
- [ ] 自定义 CSS 输入经过 sanitizeCustomCSS 校验后才生效
- [ ] 一键重置按钮将样式恢复为当前模板默认值
- [ ] 所有样式变更通过 saveAsTemplate 保存到后端
</success_criteria>

<output>
完成后创建 `.planning/phases/05-templates-export/05-02-SUMMARY.md`
</output>
